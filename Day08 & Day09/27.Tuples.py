#Create a tuple with values
sample_tuple = ("Arockia", "Kumar", "Rajesh")
print(sample_tuple)

sample_tuple1 = (1,2,4)
print(sample_tuple1)

#Identify the count of elements in a tuple
print(len(sample_tuple))

#sample_tuple[2] = "Raja"

#Access Tuple element
print(sample_tuple1[0])

# Tuple doesn't change/modify the existing element value - IMMUTABLE
sample_tuple1[0] = 5
print(sample_tuple1)
